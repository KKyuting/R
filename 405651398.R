> house=read.csv("C:/Users/405651398/Documents/house.csv")
> dim(house)
> utils:::menuInstallPkgs()
> head(house)
> price=house$price
> summary(price)
> hist(price)
> hist(price,col=rainbow(15))
> stem(price)
> mean(price)
> median(price)
> min(price)
> max(price)
> names(house)
> table(house$district)
> pie(table(house$area))
> pie(table(house$area),col=rainbow(20))
> barplot(table(house$area),col=rainbow(30))
> tapply(house$price,house$area,mean)
> sort(tapply(house$price,house$area,mean))
> library(tree)
> result=tree(price~.,data=house)
> plot(result)
> text(result)
> levels(house$area)
> result
